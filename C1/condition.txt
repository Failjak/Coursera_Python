Протокол поддерживает два вида запросов к серверу со стороны клиента:
	- отправка данных для сохранения их на сервере
	- получения сохраненных данных


Общий формат запроса клиента:
	<команда> <данные запроса><\n>

	<команда>:
		put — сохранить данные на сервере, 
		get — вернуть сохраненные данные с сервера

Общий формат ответов сервера:  
	<статус ответа><\n><данные ответа><\n\n>
	
	<статус ответа>:
		"ok"
		"error"

В случаях:
	- когда в запросе на получение данных передан не существующий ключ
	- успешного выполнения команды сохранения данных put
	=> ok\n\n

	Если нарушен формат запроса, ошибочная команда или 
	значения value и timestamp не могут быть приведены к необходимому типу 
	=> error\nwrong command\n\n


Как я понимаю, надо изначльно задать подключение к сокету.
Хотя да, это как-то не очень. Надо тогдао открыть подключение каждый раз когда ы мы обращаемся в нашим методам.
Если образаемся к пут должны записать данные, если же в гет тогда считать данные.
Очень однако интересно
ТОгда при пут мы должны передать инфу в метод, преобразовать ее в байт-код и записать
Если же гет мы должны считывать инфу после считывания вы должны верннуть нашу сроку в исходящее подключение 
